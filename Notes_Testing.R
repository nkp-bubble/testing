##function are creating using the function(), they can be treated like any other R object

##na.rm = T/F is useful to remove NAs, generally should not reverse the order of arguments.

## "..." argument: variable number of arguments, when extending another function and you don't want to copy the entire list of the original. Can also be used so that extra arguments can be passed to teh methods

